apiVersion: v1
kind: Service
metadata:
  name: {{ include "aks-openapi-operator.fullname" . }}
  labels:
    {{- include "aks-openapi-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aks-openapi-operator.selectorLabels" . | nindent 4 }}
