---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: deployapis.apim.hedinit.io
spec:
  group: apim.hedinit.io
  names:
    kind: DeployAPI
    listKind: DeployAPIList
    plural: deployapis
    singular: deployapi
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: DeployAPI is the Schema for the deployapis API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DeployAPISpec defines the desired state of DeployAPI.
            properties:
              APIID:
                type: string
              apimService:
                type: string
              host:
                description: Foo is an example field of DeployAPI. Edit deployapi_types.go
                  to remove/update
                type: string
              openApiDefinitionUrl:
                type: string
              resourceGroup:
                type: string
              revision:
                type: string
              routePrefix:
                type: string
              subscription:
                type: string
            required:
            - APIID
            - apimService
            - host
            - openApiDefinitionUrl
            - resourceGroup
            - routePrefix
            - subscription
            type: object
          status:
            description: DeployAPIStatus defines the observed state of DeployAPI.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
