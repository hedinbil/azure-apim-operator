apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "azure-apim-operator.fullname" . }}-test-connection"
  labels:
    {{- include "azure-apim-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "azure-apim-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
