{{ .Chart.Name }} {{ .Chart.Version }} is installed in namespace {{ .Release.Namespace }}.

The operator has no Service: it serves only its health probes on port 8081.

  kubectl --namespace {{ .Release.Namespace }} get pods -l app.kubernetes.io/name={{ include "azure-apim-operator.name" . }}
  kubectl --namespace {{ .Release.Namespace }} get apimservices,apimapis,apimapideployments,apimproducts,apimtags,apiminboundpolicies

Register the APIM instances the operator may manage under `apimServices` in values, and point
the ServiceAccount at a workload identity that has the API Management Service Contributor role.
