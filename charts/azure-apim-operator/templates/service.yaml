apiVersion: v1
kind: Service
metadata:
  name: {{ include "azure-apim-operator.fullname" . }}
  labels:
    {{- include "azure-apim-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "azure-apim-operator.selectorLabels" . | nindent 4 }}
