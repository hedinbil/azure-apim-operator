apiVersion: v1
kind: Service
metadata:
  name: {{ include "aks-apim-operator.fullname" . }}
  labels:
    {{- include "aks-apim-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aks-apim-operator.selectorLabels" . | nindent 4 }}
