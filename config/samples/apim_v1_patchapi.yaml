apiVersion: apim.hedinit.io/v1
kind: PatchAPI
metadata:
  labels:
    app.kubernetes.io/name: azure-apim-operator
    app.kubernetes.io/managed-by: kustomize
  name: patchapi-sample
spec:
  # TODO(user): Add fields here
