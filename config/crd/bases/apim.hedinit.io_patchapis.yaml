---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: patchapis.apim.hedinit.io
spec:
  group: apim.hedinit.io
  names:
    kind: PatchAPI
    listKind: PatchAPIList
    plural: patchapis
    singular: patchapi
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: PatchAPI is the Schema for the patchapis API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PatchAPISpec defines the desired state of PatchAPI.
            properties:
              APIID:
                description: Foo is an example field of PatchAPI. Edit patchapi_types.go
                  to remove/update
                type: string
              serviceURL:
                type: string
            required:
            - APIID
            - serviceURL
            type: object
          status:
            description: PatchAPIStatus defines the observed state of PatchAPI.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
